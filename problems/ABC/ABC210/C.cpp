#include <bits/stdc++.h>
using namespace std;

int main(){
    
    int N,K;
    cin >> N >> k;

    vector<int> C(N);
    for (int i = 0; i < N; i++){
        cin >> C.at(i);
    }

    int ans;
    ans = 0
    vector<int> l;
    for (int i = 0; i < N; i++){
        if(i <= N-K){
            l = 
        }
    }

}